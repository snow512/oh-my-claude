name: ralph-loop-run
description: >
  작업을 ralph-loop 플러그인으로 반복 실행. 종료 조건과 반복 횟수를 자동 판단하여 실행.
  사용자는 "랄프루프해 {작업내용}" 이라고만 하면 됨.
  트리거: 랄프루프해, 랄프루프, ralph loop, 랄프 돌려, 루프해, 반복실행해
tools: shell, read_file, glob, grep
